<UserControl x:Class="Eumel.Dj.Ui.Core.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Eumel.Dj.Ui.Core.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
          <Grid.RowDefinitions>
              <RowDefinition Height="*"/>
              <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions> 

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>


        <TextBox IsReadOnly="true" Name="ChatHistory" Margin="4" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" />

        <TextBox x:Name="ChatMessage" Grid.Column="0" Grid.Row="1" Margin="4" />
        <Button x:Name="SendMessage" Content="Send" Grid.Column="1" Grid.Row="1" Margin="4" />
    </Grid>
</UserControl>
