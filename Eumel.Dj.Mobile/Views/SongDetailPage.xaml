<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Eumel.Dj.Mobile.ViewModels;assembly=Eumel.Dj.Mobile"
             x:Class="Eumel.Dj.Mobile.Views.SongDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15" x:DataType="viewModels:SongDetailViewModel">
        <Label Text="Text:" FontSize="Medium" />
        <Label Text="{Binding Text}" FontSize="Small"/>
        <Label Text="Description:" FontSize="Medium" />
        <Label Text="{Binding Description}" FontSize="Small"/>
        <Label Text="Has My Vote:" FontSize="Medium" />
        <Label Text="{Binding HasMyVote}" FontSize="Small"/>
    </StackLayout>
    
</ContentPage>