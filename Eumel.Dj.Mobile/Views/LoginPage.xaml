<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:forms="clr-namespace:ZXing.Net.Mobile.Forms;assembly=ZXing.Net.Mobile.Forms"
             mc:Ignorable="d"
             x:Class="Eumel.Dj.Mobile.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <Grid Padding="10,0,10,0" VerticalOptions="Center">
            
            <!--OnScanResult="scanView_OnScanResult"-->
            <forms:ZXingScannerView x:Name="scanView"
                                    
                                    IsScanning="False"
                                    WidthRequest="300"
                                    HeightRequest="500"
                                    VerticalOptions="CenterAndExpand"
                                    HorizontalOptions="CenterAndExpand"/>
            <forms:ZXingDefaultOverlay x:Name="Overlay"
                                       TopText="Align the settings code within the frame" 
                                       BottomText="Set username before scanning QR code" />
        </Grid> 
    </ContentPage.Content>
</ContentPage>